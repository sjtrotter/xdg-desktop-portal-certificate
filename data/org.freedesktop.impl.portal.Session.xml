<?xml version="1.0"?>
<!--
  VERBATIM COPY. DO NOT EDIT THIS FILE HERE.

  Apart from this comment block, this file is copied byte for byte from

      data/org.freedesktop.impl.portal.Session.xml

  in xdg-desktop-portal itself (branch experimental/certificate-webauthentication,
  commit 703fb22; the file is unchanged from upstream main). It is the shared
  backend-side Session interface every portal backend exports, and it is
  reproduced here only because no released xdg-desktop-portal installs the impl
  interface XML where an out-of-tree backend can codegen from it. See
  docs/IMPL-INTERFACE.md.

  The file is LGPL-2.1-or-later, the same licence as this repository; see
  docs/decisions/0004-license.md.
-->
<!--
 SPDX-License-Identifier: LGPL-2.1-or-later
 SPDX-FileCopyrightText: Copyright © the xdg-desktop-portal contributors
-->

<node name="/" xmlns:doc="http://www.freedesktop.org/dbus/1.0/doc.dtd">
  <!--
      org.freedesktop.impl.portal.Session:
      @short_description: Shared session interface

      The Session interface is shared by all portal interfaces that involve
      long lived sessions.  When a method that creates a session is called, the
      reply will include a session handle (i.e. object path) for a Session
      object, which will stay alive for the duration of the session.

      The portal can abort the interaction by calling
      :ref:`org.freedesktop.impl.portal.Session.Close` on the Session object.
  -->
  <interface name="org.freedesktop.impl.portal.Session">

    <!--
        Close:

        Close the session.
    -->
    <method name="Close">
    </method>

    <!--
        Closed:

        The session was closed by the portal implementation.
    -->
    <signal name="Closed">
    </signal>
    <property name="version" type="u" access="read"/>
  </interface>
</node>
